import { LOGIN_FORM } from '../../../../config';

export const emailFieldName = `${LOGIN_FORM}.email`;
export const passwordFieldName = `${LOGIN_FORM}.password`;
export const formTypeFieldName = `${LOGIN_FORM}.formType`;
